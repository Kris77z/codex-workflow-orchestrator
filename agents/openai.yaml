interface:
  display_name: "Codex Subagents 优化器"
  short_description: "安装可复用 Codex subagent 默认配置"
  default_prompt: "使用 $codex-subagents-optimizer 为当前环境配置一套务实的 Codex subagents。"
policy:
  allow_implicit_invocation: true
